@model ICollection<TodoTaskPartner>

@if (Model != null)
{
    @if (Model.Count > 0)
    {
        @Html.Raw("with:&nbsp")
    }
    @foreach (var item in Model)
    {
        <span class="tasklist-item--partner">
            @item.Staff.FirstName
        </span>
    }
}