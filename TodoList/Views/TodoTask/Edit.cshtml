@using TodoList.Common.Utilities
@using Microsoft.EntityFrameworkCore.Metadata.Internal
@using TaskStatus = TodoList.Models.TaskStatus
@model TodoList.ViewModels.TodoTaskEditVm

@{
    ViewBag.Title = "Editing " + @Html.ViewData.Model.TodoTask.Name;
    Layout = "_Layout";
}

<h6>
    <a asp-controller="TodoTask" asp-action="Index">
        Trở về Danh sách Công việc
    </a>
</h6>

<form asp-action="Edit">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="TodoTask.Id"/>
    <input type="hidden" asp-for="TodoTask.StaffId"/>

    <div class="form-group">
        <input asp-for="TodoTask.Name" class="form-control col-form-label col-form-label-lg"/>
        <span asp-validation-for="TodoTask.Name" class="text-danger"></span>
    </div>
    <hr>

    <div class="form-row">
        <div class="form-group col">
            <label asp-for="TodoTask.StartDate" class="control-label"></label>
            <input asp-for="TodoTask.StartDate" class="form-control"/>
            <span asp-validation-for="TodoTask.StartDate" class="text-danger"></span>
        </div>
        <div class="form-group col">
            <label asp-for="TodoTask.EndDate" class="control-label"></label>
            <input asp-for="TodoTask.EndDate" class="form-control"/>
            <span asp-validation-for="TodoTask.EndDate" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group">
        <label asp-for="TodoTask.TodoTaskPartners" class="control-label"></label>
        <select asp-for="TodoTaskPartnerIds" asp-items="Model.StaffSelectList" class="form-control" multiple="multiple">
        </select>
    </div>
    <div class="form-group">
        <label asp-for="TodoTask.Status" class="control-label"></label>
        <select asp-for="TodoTask.Status" class="form-control">
            @foreach (var item in Enum.GetValues(typeof(TaskStatus)).Cast<TaskStatus>())
            {
                <option value="@item">
                    @Html.DisplayFor(model => item)
                </option>
            }
        </select>
        <span asp-validation-for="TodoTask.Status" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="TodoTask.Access" class="control-label"></label>
        <select asp-for="TodoTask.Access" class="form-control">
            @foreach (var item in Enum.GetValues(typeof(TaskAccess)).Cast<TaskAccess>())
            {
                <option value="@item">
                    @Html.DisplayFor(model => item)
                </option>
            }
        </select>
        <span asp-validation-for="TodoTask.Access" class="text-danger"></span>
    </div>
    <input type="submit" value="Lưu" class="btn btn-primary"/>
</form>

<form asp-action="Delete">
    <input type="hidden" asp-for="TodoTask.Id"/>
    <input type="submit" value="Xoá" class="btn btn-danger"/>
</form>


@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}